/**
 * Optional Tailwind mapping for AKIOR wrapper.
 * Use with the main CSS file, not instead of it.
 */
export const akiorTailwind = {
  root: "relative inline-block isolate select-none",
  asset: "absolute inset-0 h-full w-full object-contain pointer-events-none",
  fx: "absolute inset-0 rounded-full pointer-events-none",
};
